# Unitree URDF for RL
## STL Tool
use stl_tools.py to generate simple stl files

!!! the urdf cylinder will make learning iteration slow !!!

```bash
pip install numpy
pip install numpy-stl
python ./stl_tool.py
```
## Robots
* Unitree B2(no lidar; convert dae to stl; collision simplified)